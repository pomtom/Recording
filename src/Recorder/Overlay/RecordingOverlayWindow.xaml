<Window x:Class="Recorder.Overlay.RecordingOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recording"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        Focusable="False">

    <Window.Resources>
        <!-- Held as a resource rather than an inline trigger so SetState can start and stop it
             when the recording pauses. -->
        <Storyboard x:Key="PulseStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.25" Duration="0:0:0.9"
                             AutoReverse="True" RepeatBehavior="Forever" />
        </Storyboard>
    </Window.Resources>

    <Border x:Name="Root"
            CornerRadius="8"
            Padding="12,7"
            Background="#E6161719"
            BorderBrush="#33FFFFFF"
            BorderThickness="1"
            Cursor="SizeAll">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.45" Color="#000000" />
        </Border.Effect>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Ellipse x:Name="Dot" Width="11" Height="11" Fill="#E83E3E" VerticalAlignment="Center" />

            <TextBlock x:Name="Label"
                       Text="REC"
                       Margin="8,0,0,0"
                       Foreground="#F2F2F2"
                       FontFamily="Segoe UI"
                       FontSize="12"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />

            <TextBlock x:Name="TimeText"
                       Text="00:00:00"
                       Margin="8,0,0,0"
                       Foreground="#F2F2F2"
                       FontFamily="Consolas, Segoe UI"
                       FontSize="13"
                       VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</Window>
